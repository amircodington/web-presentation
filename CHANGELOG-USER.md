# تغییرات نسخه‌ها

<div dir="rtl">

آن‌چه در هر نسخه از برنامه تغییر کرده است، به زبان ساده.

> این فایل در هر انتشار به‌صورت دستی نوشته می‌شود. جزئیات فنی در
> [`CHANGELOG.md`](CHANGELOG.md) است.

## منتشرنشده

## نسخه 0.14.1 — ۱۲ شهریور ۱۴۰۵

### تست نهایی
- نسخه نهایی این دور از اصلاحات. کل برنامه در اندازه واقعی تلویزیون (۱۹۲۰×۱۰۸۰) تست شد:
  - همه صفحه‌ها باز می‌شوند و هرکدام دقیقاً به همان صفحه‌ای می‌رسند که باید.
  - نوار پایین صفحه و زیرنویس روی هیچ متنی نمی‌افتند.
  - هیچ نوشته‌ای از کارتش بیرون نمی‌زند و هیچ دکمه‌ای بیرون از قاب نیست.
  - همه بازی‌ها تا نتیجه بازی شدند و همه نتیجه نشان دادند.
  - بازگشت خودکار به صفحه اول بعد از بی‌کاری کار می‌کند و اطلاعات بازدیدکننده قبلی پاک می‌شود.
  - هیچ خطایی در مرورگر ثبت نشد.

### نیاز به تیم
- عکس مناسب گروه بزرگسال نداریم؛ همه عکس‌های موجود از دانش‌آموزان و نوجوانان است.
- قیمت کلاس کودک هنوز مشخص نیست، فعلاً روی کارتش نوشته «قیمت را در غرفه بپرسید».
- تست نهایی روی خود تلویزیون ۷۵ اینچی لمسی هنوز انجام نشده و طبق بریف باید قبل از رویداد انجام شود.

## نسخه 0.14.0 — ۱۲ شهریور ۱۴۰۵

### تازه
- بازی «قسط واقعاً ارزون‌تره؟» حالا به سؤالی که خودش ایجاد می‌کرد جواب می‌دهد. بعد از اینکه هزینه کل را نشان داد، یک صفحه جدید می‌گوید قسطی خریدن در چه شرایطی جواب می‌دهد و اگر آن شرط‌ها برقرار نیست، به‌جایش چه کارهایی می‌شود کرد.
- بازی «قدرت خریدت چقدر مقاومه؟» حالا بودجه‌ای را که خود بازدیدکننده ساخته تحلیل می‌کند، نه فقط جواب چهارگزینه‌ای را. اول عددهای خودش را نشان می‌دهد — خرج ماه، حاشیه امن، و آنچه کم کرده — و بعد دو سه نکته دقیقاً درباره همان بودجه: بسته شد یا نه، آموزش را صفر کرد یا نه، همه هزینه‌های غیرضروری را برداشت یا نه.
- نتیجه «چالش هوش مالی» نوجوان هم مثل بزرگسال «نقطه قوت»، «نقطه قابل بهبود» و «چارچوب پیشنهادی» گرفت، به زبان خودش.

### بهتر شد
- «چالش ۱۰۰ میلیون» دیگر همان بازی «قلک من» با اسم دیگر نیست. سکه‌های خندان و شخصیت‌های کارتونی جایشان را به نشانه‌های ساده و ژتون دادند؛ همان بازی، ولی متناسب با سن نوجوان.
- بالای صفحه هیچ بازی‌ای در دنیای بزرگسال دیگر شخصیت کارتونی ندارد.
- سؤال پایانی بازی بودجه بازنویسی شد تا به خود بازی ربط داشته باشد.

### بررسی شد، مشکلی نبود
- نقطه‌های پیشرفت «سؤال ۱ از ۶» بررسی شدند: ترتیبشان برای فارسی درست است و نقطه فعال سمت راست قرار می‌گیرد.

## نسخه 0.13.0 — ۱۲ شهریور ۱۴۰۵

### تازه
- سه دنیا حالا واقعاً سه فضای متفاوت‌اند، نه یک صفحه در سه رنگ:
  - **کودک** همان شخصیت‌های بامزه و کارت‌های ضخیم را نگه داشت.
  - **نوجوان** شبیه یک بازی شد: کارت‌ها «مرحله ۱» تا «مرحله ۴» هستند، با نشانه‌های ساده و گرافیکی به‌جای شخصیت‌های کارتونی.
  - **بزرگسال** جدی و آرام شد: کارت‌ها شبیه یک سند خط‌کشی‌شده‌اند، با شماره‌گذاری «۱ / ۵» و آیکون‌های نازک. هیچ شخصیت کارتونی‌ای در دنیای بزرگسال باقی نماند.
- رنگ اصلی دنیای بزرگسال از طلایی به سبزِ لوگوی باشگاه تغییر کرد. طلایی همان‌جایی ماند که معنی «پول» می‌دهد.
- پشت عنوان دنیای بزرگسال یک عکس واقعی از محل برگزاری، بسیار کم‌رنگ، اضافه شد.

### نیاز به تیم محتوا
- در پوشه تصاویر هیچ عکسی از مخاطب بزرگسال نداریم؛ همه عکس‌ها از دانش‌آموزان و نوجوانان است. اگر می‌خواهید دنیای بزرگسال عکس بیشتری داشته باشد، لطفاً چند عکس مناسب این گروه سنی تهیه کنید.

## نسخه 0.12.0 — ۱۲ شهریور ۱۴۰۵

### درست شد
- صفحه اول و صفحه انتخاب گروه هر دو یک سؤال می‌پرسیدند و هر دو همان سه کارت را نشان می‌دادند؛ یعنی بازدیدکننده یک بار انتخاب می‌کرد و بلافاصله دوباره از او پرسیده می‌شد. حالا صفحه اول فقط سه دنیا را *نشان می‌دهد* و انتخاب فقط در صفحه بعد است.
- در صفحه اول، سکه پیش از رسیدن به هر دنیا شکلش عوض می‌شد — موشک یک ثانیه‌ونیم جلوی در دنیای کودک می‌ایستاد. حالا سکه اول می‌رسد و بعد تبدیل می‌شود.
- اسم «بزرگسال» زیر درِ آن دنیا تقریباً نامرئی بود، چون با رنگ متن همان دنیا روی زمینه صفحه اصلی نوشته می‌شد.
- نوشته‌هایی که برنامه می‌گوید (زیرنویس) روی کارت‌های دنیای کودک می‌افتاد. فاصله‌ای که هر صفحه برای نوار پایین خالی می‌گذارد حالا از روی اندازه واقعی خود نوار حساب می‌شود، نه یک عدد ثابت — پس روی هر اندازه صفحه‌ای درست است.

### بهتر شد
- صفحه اول زنده‌تر شد: سه دنیا حالا سه «در» هستند که پول از میانشان می‌گذرد، و دکمه «برای شروع، صفحه را لمس کن» بزرگ‌ترین چیز روی صفحه است.
- نوار پایین صفحه تمیزتر شد: یک جداکننده بین دکمه‌های رفت‌وبرگشت و دکمه صدا، حالتِ روشن‌وخاموش واضح برای صدا، و اندازه دکمه‌ها به حداقل استاندارد لمس رسید.
- صفحه دنیای بزرگسال از نو چیده شد: سؤال کنکور به بالای صفحه رفت و «پروفایل تصمیم‌گیری مالی» به‌جای یک نوار جدا، آخرین ایستگاه مسیر شد. حالا هر پنج کارت در یک ردیف و کامل دیده می‌شوند.

## نسخه 0.11.0 — ۱۲ شهریور ۱۴۰۵

### درست شد
- امتیاز بازی‌های «راز نوسان قیمت» و «فرصته یا کلاهبرداری؟» با لمس دوباره خراب می‌شد. روی صفحه لمسی یک انگشت که دو بار بخورد، یا کسی که وسط بازی دستش را جلو بیاورد، همان سؤال را دوباره امتیاز می‌داد — یک بازی پنج‌سؤالی می‌توانست «۱۰ از ۵» نشان بدهد. حالا هر سؤال فقط یک جواب دارد و لمس دوم هیچ کاری نمی‌کند.
- بازی «قلک من» بعضی وقت‌ها نتیجه‌ای نشان نمی‌داد: نمودار می‌آمد ولی زیرش هیچ متنی نبود و به نظر می‌رسید بازی خراب شده. مثلاً وقتی ۵ سکه خرج، ۳ سکه پس‌انداز و ۲ سکه برای هدف می‌گذاشتید. حالا برای هر تقسیمی حرفی برای گفتن هست. همین مشکل «چالش ۱۰۰ میلیون» و «سبدت رو بچین» را هم داشت.
- دکمه «جزئیات» کلاس «سواد مالی مقدماتی کودک» هیچ کاری نمی‌کرد، چون صفحه معرفی این کلاس اصلاً ساخته نشده بود. حالا صفحه کامل کلاس با سرفصل‌ها، مخاطب و محل برگزاری باز می‌شود.
- در دنیای بزرگسال، توضیح زیر کارت‌های بازی از پایین کارت بیرون می‌زد و روی زمینه صفحه می‌افتاد. حالا همه‌چیز داخل کارت جا می‌شود و تصویر هر چهار کارت هم‌اندازه است.

### بهتر شد
- معرفی کلاس کودک تصویر مخصوص خودش را گرفت، تا هیچ دو صفحه‌ای یک عکس تکراری نشان ندهند.

## نسخه 0.10.2 — ۱۱ شهریور ۱۴۰۵

### درست شد
- در صفحه انتخاب گروه، دکمه «بزن بریم» پایین هر سه کارت از لبه کارت بیرون می‌زد و دیده نمی‌شد — یعنی تنها دکمه‌ای که بازدیدکننده باید بزند. اندازه‌ها تنظیم شد و حالا هر سه کارت کامل جا می‌شوند.

## نسخه 0.10.1 — ۱۱ شهریور ۱۴۰۵

### درست شد
- چند دکمه بودند که هیچ کاری نمی‌کردند: دکمه‌ی صفحه پیشنهاد ویژه به یک صفحه‌ی حذف‌شده وصل بود، و صفحه‌ی چالش‌های زنده به بازی‌هایی وصل بود که جایشان عوض شده بود. روی یک دستگاه بدون اپراتور، دکمه‌ای که کار نمی‌کند از خطا هم بدتر است، چون کسی متوجهش نمی‌شود.
- فهرست دوره‌ها که دیگر از هیچ‌جا باز نمی‌شد حذف شد. حالا معرفی هر دوره مستقیماً از صفحه پیشنهاد هر دنیا باز می‌شود.

### بهتر شد
- نوار پایین صفحه ساده شد: فقط «بازگشت»، «خانه» و «صدا». دکمه «نقشه کامل» حذف شد — بازدیدکننده‌ای که وسط یک سالن شلوغ ایستاده یک سؤال بیشتر ندارد: چطور برگردم.

### تست نهایی
- کل مسیر روی مرورگر و در اندازه واقعی تلویزیون (۱۹۲۰×۱۰۸۰) تست شد: هر سه دنیا، رنگ مخصوص هرکدام، بازگشت خودکار به صفحه اول بعد از بی‌کاری و پاک شدن اطلاعات بازدیدکننده قبلی، بارگذاری همه فایل‌ها، و بدون هیچ خطایی.

## نسخه 0.10.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- دنیای بزرگسال کامل شد و چهار تجربه دارد:
  - **قدرت خریدت چقدر مقاومه؟** — بودجه یک خانوار روی صفحه است، هزینه‌های ضروری بالا می‌روند و کاربر باید بودجه را بازنگری کند. بعد می‌پرسیم اولین واکنش منطقی‌اش چیست.
  - **همه پولت یک جاست؟** — تقسیم دارایی و بررسی نقدشوندگی و تمرکز ریسک.
  - **قسط واقعاً ارزون‌تره؟** — کاربر اول حدس می‌زند مجموع اقساط چقدر می‌شود، بعد پرداخت‌ها جلوی چشمش روی هم جمع می‌شوند و از حدسش رد می‌شوند.
  - **خبر داغ، تصمیم سرد** — چند خبر داغ بازار و اینکه کاربر با هرکدام چه می‌کند.
- **پروفایل تصمیم‌گیری مالی** جایگزین تست چهار سؤالی قدیمی شد. نتیجه دیگر یک نمره تنها نیست: هفت بُعد جداگانه (مدیریت نقدینگی، تاب‌آوری مالی، مدیریت ریسک، شناخت هزینه بدهی، مقاومت در برابر FOMO، تشخیص کلاهبرداری، کیفیت تصمیم) به‌علاوه سه بخش «نقطه قوت»، «نقطه قابل بهبود» و «چارچوب پیشنهادی».
- سؤال «امسال کنکور دادی؟» به صفحه اصلی بزرگسال اضافه شد و بر اساس جوابش، دوره ۱۸+ جلوتر پیشنهاد می‌شود.

### مهم برای تیم محتوا
- همه سؤال‌های بزرگسال در یک فایل جداگانه به اسم `content/adult-scenarios.json` هستند. برای عوض کردن، اضافه کردن یا حذف سؤال، فقط همین فایل را ویرایش کنید — نیازی به برنامه‌نویس نیست. تاریخ آخرین به‌روزرسانی هم بالای همان فایل نوشته می‌شود.
- هیچ عدد اقتصادی ثابتی (مثل درصد تورم) در محتوا نوشته نشده تا سریع تاریخ مصرف پیدا نکند.
- زیر همه نتیجه‌ها نوشته شده که این نتیجه فقط آموزشی است و توصیه خرید یا فروش نیست.

### حذف شد
- تست عمومی چهار سؤالی که قبلاً قبل از هر بازی نمایش داده می‌شد.

## نسخه 0.9.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- **چالش هوش مالی** برای نوجوان‌ها اضافه شد: شش موقعیت واقعی (خرید نقدی یا قسطی، انتخاب بین دو سرمایه‌گذاری، خرج کردن یا یاد گرفتن، تشخیص کلاهبرداری، خبر داغ بازار، و پولی که زود لازمش داری). هیچ جوابی «غلط» نیست؛ هر انتخاب چیزی درباره‌ی خود نوجوان می‌گوید.
- در پایان، امتیاز از ۱۰۰ روی صفحه بالا می‌رود تا به عدد نهایی برسد، و سطح نوجوان اعلام می‌شود: «شروع مسیر»، «پول‌بلد در حال رشد»، «تصمیم‌گیر مالی» یا «استراتژیست مالی».
- کنار امتیاز، چهار نوار نشان می‌دهد در کدام بخش قوی‌تر است: مدیریت پول، مدیریت ریسک، شناخت بازار، کیفیت تصمیم.
- در «چالش ۱۰۰ میلیون» حالا بعد از تقسیم پول یک سؤال می‌آید: «این پول رو تا کی لازم نداری؟» و نتیجه بر اساس همین جواب داده می‌شود — یعنی به‌جای اینکه بگوییم انتخابت درست بود یا نه، می‌گوییم با افق زمانی خودت جور بود یا نه.
- صفحه‌ی پیشنهاد مسیر برای نوجوان اضافه شد: اول «مسیر ثروت»، بعد «مدرسه کسب‌وکار».

### درست شد
- در دنیای نوجوان و بزرگسال، دکمه‌های کم‌رنگ (مثل «دوباره») هم‌رنگ زمینه‌شان بودند و دیده نمی‌شدند.

### نکته
- زیر نتیجه نوشته شده که این نتیجه فقط آموزشی است و توصیه خرید یا فروش نیست.

## نسخه 0.8.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- دنیای کودک کامل شد و حالا چهار بازی دارد:
  - **نیاز یا خواسته؟** — بچه هر چیزی (آب، غذا، کفش، بستنی، کنسول، دوچرخه و…) را می‌کشد توی «نیاز» یا «خواسته». بعضی چیزها جواب قطعی ندارند و بازی می‌گوید «گاهی جواب به شرایط بستگی داره!» — این خودش یکی از درس‌هاست.
  - **قلک من** — ده سکه، سه جا: خرج، پس‌انداز، و هدف.
  - **فروشگاه کوچک** — صد سکه و یک فروشگاه. کیف پول جلوی چشم بچه خالی می‌شود و اگر پول کم بیاورد، به‌جای پیغام خطا می‌گوید «پولت کافیه؟ یه بار دیگه حساب کن!».
  - **کسب‌وکار کوچولوی من** — بچه یک غرفه می‌زند (آبمیوه، کوکی، دستبند یا نقاشی)، قیمت می‌گذارد و مشتری‌ها یکی‌یکی می‌آیند و واکنش نشان می‌دهند.
- بعد از هر بازی، یک جشن تمام‌صفحه با کاغذرنگی، بارش سکه و یک نشان مخصوص همان بازی («قهرمان پس‌انداز»، «استاد انتخاب هوشمند»، «خریدار باهوش»، «کسب‌وکارچی کوچولو»). جشن خودش بعد از چند ثانیه بسته می‌شود.
- کلاس «سواد مالی مقدماتی کودک» اضافه شد. این کلاس در نسخه قبلی اصلاً روی تلویزیون دیده نمی‌شد.

### بهتر شد
- معرفی کلاس‌ها فقط بعد از این نشان داده می‌شود که بچه حداقل یک بازی را انجام داده باشد؛ اول تجربه، بعد پیشنهاد.
- برای بازی‌های کودک ۱۴ تصویر جدید کشیده شد (آب، غذا، کفش، کتاب، اسباب‌بازی، کنسول، بستنی، دوچرخه، کیف، مداد، آبمیوه، دستبند، نقاشی، کوکی).

## نسخه 0.7.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- تلویزیون حالا صدا دارد. هر کاری که بازدیدکننده می‌کند صدای خودش را دارد: زدن روی دکمه، افتادن سکه، جواب درست، جواب اشتباه، و باز شدن نتیجه.
- هر دنیا صدای خودش را دارد. همان زدن روی دکمه، در دنیای کودک صدای بامزه می‌دهد، در دنیای نوجوان صدای دیجیتال، و در دنیای بزرگسال یک کلیک خیلی آرام.
- بلندی صدا هم برای هر دنیا جداست: کودک بلندتر، نوجوان متوسط، بزرگسال خیلی آرام.
- دکمه قطع/وصل صدا در نوار پایین صفحه اضافه شد.
- هر جمله‌ای که قرار است گفته شود، نوشته‌اش هم پایین صفحه می‌آید؛ پس با صدای خاموش یا در سالن شلوغ هیچ‌چیز از دست نمی‌رود.
- در دنیای کودک، موقع ورود جمله «بزن بریم بازی کنیم!» نمایش داده می‌شود.

### نکته برای تیم
- همه صداهای افکتی ساخته و روی خود دستگاه ذخیره شده‌اند؛ به اینترنت نیازی نیست.
- جمله‌های گفتاری (صدای گوینده) هنوز ضبط نشده‌اند. متن‌شان آماده است و روی صفحه نوشته می‌شود؛ هر وقت فایل صوتی‌شان را دادید، فقط باید کنار همان متن گذاشته شود.
- صدای دستگاه تا وقتی کسی برای اولین بار صفحه را لمس نکند پخش نمی‌شود؛ این محدودیت خود مرورگرهاست، نه ایراد برنامه.

## نسخه 0.6.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- صفحه اول (وقتی کسی با دستگاه کار نمی‌کند) دیگر یک پوستر ثابت نیست. حالا یک انیمیشن زنده است: یک سکه وارد دنیای کودک می‌شود و قلک می‌شود، بعد وارد دنیای نوجوان می‌شود و به موشک تبدیل می‌شود، و آخر در دنیای بزرگسال شمش طلا می‌شود. هر دنیا هم به نوبت با رنگ خودش روشن می‌شود.
- هر دنیا یک جمله کوتاه مخصوص خودش دارد که وقتی نوبتش می‌شود زیر نامش می‌آید.
- روی هر جای صفحه که بزنید وارد می‌شوید، نه فقط روی دکمه.

### بهتر شد
- جمله بزرگ وسط صفحه شد «تو کدوم دنیایی؟» و زیرش «صفحه رو لمس کن» که آرام نفس می‌کشد تا از دور دیده شود.
- طول یک دور کامل انیمیشن حالا از تنظیمات قابل تغییر است (پیش‌فرض ۱۸ ثانیه).

## نسخه 0.5.0 — ۱۱ شهریور ۱۴۰۵

### تازه
- صفحه اول تعاملی عوض شد. حالا اول از بازدیدکننده می‌پرسیم «تو توی کدوم گروهی؟» و سه انتخاب بیشتر ندارد: کودک و دبستان، نوجوان و دبیرستان، بزرگسال.
- هر گروه دنیای خودش را دارد؛ با رنگ، حال‌وهوا و بازی‌های مخصوص خودش. با انتخاب گروه، کل صفحه رنگ عوض می‌کند و بازدیدکننده حس می‌کند وارد فضای خودش شده.
- بازی جدید «قلک من» برای بچه‌ها اضافه شد: ده سکه، و سه جا برای گذاشتنشان.
- مدرسه‌ها و سازمان‌ها دکمه جداگانه خودشان را زیر سه کارت اصلی دارند و دیگر با گروه‌های سنی قاطی نمی‌شوند.

### بهتر شد
- فهرست دوره‌ها دیگر قبل از بازی و تست نشان داده نمی‌شود؛ اول تجربه، بعد نتیجه، بعد پیشنهاد.
- نقشه کامل حالا هر سه دنیا را با رنگ خودشان کنار هم نشان می‌دهد.
- پایان هر بازی به جای اینکه بازدیدکننده را جای دیگری ببرد، به دنیای خودش برمی‌گرداند تا بتواند بازی بعدی را هم بزند.

### درست شد
- در دنیای نوجوان و بزرگسال، نوشته‌های دکمه‌های پایین صفحه و برچسب‌ها هم‌رنگ زمینه‌شان بودند و خوانده نمی‌شدند.
- دکمه «همکاری با مدارس و سازمان‌ها» زیر نوار پایین صفحه می‌رفت.

## نسخه 0.4.0 — ۱۰ شهریور ۱۴۰۵

- **تبلت ثبت لید غرفه اضافه شد.** با لینک `/booth?token=…` باز می‌شود و همان رمز آرشیو را می‌خواهد.
- حالا می‌شود **هر مخاطبی** را ثبت کرد — نوجوان، والد، تازه‌کنکوری، مدرسه، سازمان یا سایر — نه فقط مدرسه و سازمان.
- برای هر نفر: نام، موبایل، علاقه‌مندی‌ها، وضعیت پیگیری (آماده ثبت‌نام / نیاز به پیگیری / درخواست همکاری / فقط اطلاعات) و یادداشت کوتاه.
- شمارندهٔ «لید امروز» و فهرست آخرین ثبت‌ها بالای صفحه است، و اگر شماره‌ای تکراری باشد هشدار می‌دهد.
- **اگر اینترنت غرفه قطع شود لید از بین نمی‌رود:** روی همان تبلت نگه داشته می‌شود و با وصل شدن دوباره خودکار ارسال می‌شود. تا وقتی نوار زرد عدد نشان می‌دهد، تب را نبندید.
- خروجی CSV، بکاپ JSON و PDF از همان صفحه گرفته می‌شود.
- همهٔ لیدها — چه از تلویزیون، چه از تبلت — در یک آرشیو کنار هم دیده می‌شوند.
- رفع اشکال: ساعت در فایل CSV به وقت تهران نوشته می‌شود. قبلاً به وقت گرینویچ بود و مثلاً ۱۷:۴۵ را ۱۴:۱۵ ثبت می‌کرد.

## نسخه 0.3.0 — ۱۰ شهریور ۱۴۰۵

- ظاهر کیوسک کاملاً عوض شد: از میز سبز تیره به یک **صفحهٔ بازی رنگی و روشن**، مناسب بچه‌ها و نوجوان‌ها.
- شش شخصیت جدید وارد بازی شدند — قلک، موشک، مغازه، کتاب، شمش طلا و کیف خرید — و **حالت چهره‌شان به انتخاب بازیکن واکنش نشان می‌دهد**. اگر همهٔ پول را روی یکی بگذاری، آن شخصیت گیج می‌شود.
- در «چالش ۱۰۰ میلیون» و «سبدت رو بچین» حالا می‌شود **سکه‌ها را با انگشت کشید و روی هر گزینه انداخت**. زدن روی گزینه هم مثل قبل کار می‌کند.
- در «راز نوسان قیمت» و «فرصته یا کلاهبرداری؟» یک شخصیت به جواب درست یا غلط واکنش نشان می‌دهد.
- صفحهٔ اول حالا **دکمهٔ «بازی کنیم!»** دارد که مستقیم به بازی‌ها می‌رود، کنار دکمهٔ آزمون ۶۰ ثانیه‌ای.
- آیکون‌ها متحرک شدند: پول می‌افتد، نمودار بالا می‌رود، ساعت تیک می‌زند.
- رفع اشکال: در بعضی صفحه‌ها بعد از زدن دکمه، کل تصویر کمی از وسط خارج می‌شد. برطرف شد.
- رفع اشکال: دکمهٔ پایین بعضی بازی‌ها زیر نوار پیمایش می‌رفت و قابل زدن نبود.

## نسخه 0.2.0 — ۱۰ شهریور ۱۴۰۵

### ✨ جدید
- در صفحه‌های «همکاری با مدارس» و «همکاری با سازمان‌ها» دکمه «ثبت درخواست همکاری» اضافه شد.
  مدیر مدرسه یا مدیر سازمان همان‌جا روی صفحه نام، سمت، شماره موبایل و برنامه‌ای که
  می‌خواهد را وارد می‌کند و شماره‌اش برای تماس ذخیره می‌شود.
- درخواست‌ها روی خود دستگاه ذخیره می‌شوند و با خاموش و روشن شدن یا به‌روزرسانی برنامه
  پاک نمی‌شوند.
- یک صفحه جداگانه برای دیدن درخواست‌ها ساخته شد که فقط با لینک مخصوص باز می‌شود.
  در آن صفحه می‌توانید همه درخواست‌ها را به شکل فایل PDF فارسی یا فایل اکسل (CSV)
  بگیرید، هر درخواست را جداگانه دانلود کنید، و هر کدام یا همه را حذف کنید.
  روی خود نمایشگر هیچ دکمه‌ای به این صفحه نمی‌رود.

### 🛠 بهبود
- دکمه دریافت کد QR در همان صفحه‌ها به «دریافت کد QR» تغییر نام داد تا با دکمه ثبت
  درخواست اشتباه گرفته نشود.

## نسخه ۰.۱.۰ — ۹ شهریور ۱۴۰۵

### ✨ جدید
- نمایشگر آماده شد: صفحه خوش‌آمد، معرفی مخاطبان، دوره‌ها، کارگاه‌ها، پیشنهاد جشنواره،
  آزمون کوتاه مالی و صفحه کد QR.
- حرکت بین بخش‌ها به‌صورت سینمایی است؛ تصویر روی صفحه بزرگ جابه‌جا و زوم می‌شود.
- اگر کسی چند لحظه با صفحه کار نکند، برنامه خودش به صفحه اول برمی‌گردد و اطلاعات نفر قبل
  پاک می‌شود.
- کدهای QR روی خود دستگاه ساخته می‌شوند، بنابراین بدون اینترنت هم کار می‌کنند.
- متن‌ها، قیمت‌ها، تاریخ‌ها و رنگ‌ها همه از فایل‌های محتوا خوانده می‌شوند و بدون برنامه‌نویس
  قابل تغییر هستند.
- دکمه «نقشه کامل» اضافه شد: با یک لمس همه بخش‌ها را کنار هم می‌بینید و با لمس هر بخش
  مستقیم به آن می‌روید.
- صفحه‌های جدید: معرفی کامل دوره ۱۸+، Business School و مسیر ثروت، فعالیت‌های زنده غرفه،
  و همکاری با مدرسه و سازمان.
- تصویر روی هر نمایشگری دقیقاً با همان نسبت لپ‌تاپ نمایش داده می‌شود؛ چیزی بریده یا کشیده
  نمی‌شود.

### 🛠 بهبود
- شماره تماس، تلگرام، بله و آدرس سایت واقعی باشگاه ثروت در برنامه قرار گرفت.
- دکمه «خانه» حالا به صفحه اصلی می‌رود، نه به صفحه خوش‌آمد.

- سه بازی غرفه حالا روی خود تلویزیون قابل انجام است: «چالش ۱۰۰ میلیون»، «راز نوسان قیمت»
  و «فرصته یا کلاهبرداری؟»، به‌علاوه «سبدت رو بچین». از صفحه «فعالیت‌های زنده» روی هر کدام
  بزنید.
- ظاهر برنامه روشن و پرانرژی شد؛ رنگ قرمز فقط برای چیزی به کار می‌رود که باید لمس شود.

### 🐞 رفع اشکال
- دکمه‌های پایین صفحه دیگر روی متن‌ها نمی‌افتند.
- صفحه‌ها دیگر از سمت چپ نمایشگر بیرون نمی‌زنند و دقیقاً وسط قرار می‌گیرند.

</div>
