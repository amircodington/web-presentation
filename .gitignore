# OS
.DS_Store

# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Build / cache
dist/
*.tsbuildinfo
.turbo/
coverage/
playwright-report/
test-results/

# Environment — .env is the versioned single source of truth and IS committed.
# Only secret overlays are ignored.
.env.local
.env.*.local
.env.secrets

# Logs
*.log
npm-debug.log*

# Playwright browsers cache
.cache/

# Exported image archives
docker-tar

# The lead archive, when running outside a container. In production it is a
# named Docker volume; see docs/operations/07-lead-archive.md.
.data/
