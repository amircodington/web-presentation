# Headers every proxied location needs. Included, not repeated: a `location`
# block that sets any proxy_set_header of its own discards the ones inherited
# from its parent, which is the classic way these get silently lost.

proxy_set_header  Host               $host;
proxy_set_header  X-Real-IP          $remote_addr;
proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
proxy_set_header  X-Forwarded-Proto  $scheme;
proxy_set_header  X-Forwarded-Host   $host;
proxy_set_header  X-Forwarded-Port   $server_port;

# Next.js uses websockets for dev HMR and keeps them available in production.
proxy_set_header  Upgrade     $http_upgrade;
proxy_set_header  Connection  $connection_upgrade;
